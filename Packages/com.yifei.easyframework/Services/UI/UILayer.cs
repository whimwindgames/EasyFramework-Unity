namespace EasyFramework.Services.UI
{
    /// <summary>UI 四层(绘制叠放自下而上:Hud < Window < Popup < Overlay)。</summary>
    public enum UILayer
    {
        Hud,
        Window,
        Popup,
        Overlay,
    }
}
